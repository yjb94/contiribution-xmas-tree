import { SafeAreaView } from "react-native";
import Tree from "./components/Tree/Tree";

const Main = () => {
  return (
    <SafeAreaView>
      <Tree />
    </SafeAreaView>
  );
};

export { Main };
